apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: magic-caddy-config
  name: magic-caddy-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
