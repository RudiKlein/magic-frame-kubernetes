apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: magic-configs
  name: magic-configs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
