apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: magic-pgdata
  name: magic-pgdata
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
